#ifndef CHESS_H
#define CHESS_H

#include <QMainWindow>

class Chess : public QMainWindow
{
    Q_OBJECT
    Q_DISABLE_COPY(Chess)
public:
    explicit Chess(QWidget *parent = 0);

signals:

public slots:

};

#endif // CHESS_H
